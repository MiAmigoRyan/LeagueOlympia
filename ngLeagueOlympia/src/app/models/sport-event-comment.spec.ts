import { SportEventComment } from './sport-event-comment';

describe('SportEventComment', () => {
  it('should create an instance', () => {
    expect(new SportEventComment()).toBeTruthy();
  });
});
