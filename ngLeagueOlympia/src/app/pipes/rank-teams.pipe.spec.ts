import { RankTeamsPipe } from './rank-teams.pipe';

describe('RankTeamsPipe', () => {
  it('create an instance', () => {
    const pipe = new RankTeamsPipe();
    expect(pipe).toBeTruthy();
  });
});
